# 风眼 · Typhoon Eye —— 项目计划书

> 台风季，信息要像灯塔一样：一眼可见、一句能懂、一步能做。
> 本项目提供台风实况信息的极简呈现，并按预警等级给出可执行的应急预案，帮助沿海居民在风暴来临前做对每一步。

- 仓库：https://github.com/Mr-Salticidae/typhoon-eye （public）
- 状态：v0.8（已上线，实况 + 洪涝预警双维度）
- 立项日期：2026-07-10

---

## 一、背景与目标

每年 7–9 月是中国东南沿海台风登陆高峰。权威数据并不缺（中央气象台、各省水利厅都有发布），缺的是**普通人打开就能看懂、看完就知道该做什么**的页面：

- 官方页面信息密度高，老人与着急的人抓不到重点；
- "该不该封窗、该不该囤水、什么时候必须撤离"没有和预警等级直接挂钩；
- 移动端体验参差。

**目标**：做一个零依赖、秒开、离线可用的静态单页——

1. **看得懂**：台风叫什么、在哪、多强、往哪走、什么时候到，五个问题一屏答完；
2. **做得到**：按蓝→黄→橙→红四级预警给出递进式行动清单，勾选即存；
3. **信得过**：页面显著标注数据时效与来源，并始终引导用户以官方预警为最终依据。

## 二、用户与场景

| 用户 | 场景 | 关键诉求 |
|---|---|---|
| 沿海城市居民 | 台风预警发布后打开查看 | 强度、到达时间、我该做什么 |
| 在外子女 | 替老家父母查看并转述 | 一句话结论、可转发 |
| 社区工作者 | 组织防台准备 | 分级清单可当检查表用 |

## 三、功能范围

### v0.1 demo（本期交付）

- **实况面板**：台风名称/编号、中心位置、最大风力与风速、中心气压、移向移速、风圈半径；
- **路径可视化**：SVG 简化海岸线图，历史路径实线 + 预报路径虚线，当前位置脉动标记，逐点悬停查看时间与强度；
- **预警等级**：蓝/黄/橙/红四级切换，颜色与官方预警信号体系一致；
- **分级预案**：每级一份递进式行动清单（含上一级全部事项），可勾选，进度存 localStorage；
- **地区特点预案**：用户可手动多选沿海、海岛、城市低洼区、山区河谷、江河湖库、内陆乡村等公开风险场景，按预警等级递进补充建议；不读取或推断用户位置；
- **应急信息**：报警/急救/水上搜救电话、权威信息源直达链接；
- **双主题**：昼/夜皮肤，延续「蛛网之上」的纸墨设计体系；
- **演示数据**：v0.1 使用内置示例台风数据，页面显著标注"演示"。

### v0.2+（后续迭代，不在本期）

- 真实数据接入：GitHub Actions 定时抓取中央气象台/浙江台风路径系统公开数据，生成静态 JSON（绕开浏览器 CORS，且对源站零压力）；
- 多台风并存切换；用户手动指定公开城市后的"距该城市多远"（不读取设备位置）；预案清单打印版；PWA 离线缓存。

## 四、数据源策略

| 阶段 | 来源 | 方式 |
|---|---|---|
| v0.1 | 内置演示数据 `assets/data.js` | 结构对齐真实 API 字段，换源零改动 |
| v0.2 | 中央气象台 nmc.cn / 浙江省台风路径实时发布系统 | Actions 定时任务抓取 → 仓库内静态 JSON → Pages 分发 |

**红线**：本项目是信息聚合与科普工具，不是预警发布渠道。页面永远标注数据时间戳，并置顶提示"以当地政府与气象部门发布为准"。

## 五、设计语言

与「蛛网之上」同源：暖白纸面 / 墨色文字 / 单一强调色 + 细线分隔的杂志式排版；衬线标题、无衬线正文；`data-theme` 双皮肤。

差异点：强调色由蛛丝紫改为**风暴青**（海洋气象的色彩记忆），预警四色严格使用官方信号色，不做风格化，保证语义可靠。

极简、大气、内敛，但在预案区允许"热情"——用鼓励性的文案陪伴用户完成准备（"做完这几件事，你已经比 90% 的人更安全"）。

## 六、技术方案

- 纯静态：`index.html + assets/{style.css, app.js, data.js}`，零构建、零依赖、`file://` 双击可开；
- 路径图为手绘简化 SVG（非精确地图），只表达空间趋势，图上注明"示意"；
- 部署：GitHub Pages（main 分支根目录）；
- 兼容：现代浏览器 + 移动端优先的响应式；`prefers-reduced-motion` 降级动画。

## 七、里程碑

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 | 立项、建仓、计划书 | ✅ 本次 |
| M2 | demo 页面（实况/路径/预案/双主题） | ✅ 本次 |
| M3 | 自审校验并修复 bug | ✅ 本次 |
| M4 | 真实数据管道 + Pages 上线 | ✅ 2026-07-10 |
| M5 | 多源交叉校验（中央气象台 / 浙江 / JMA） | ✅ 2026-07-23 |
| M6 | 远洋视图 + 可缩放全球小地图（v0.6） | ✅ 2026-07-29 |
| M7 | 无定位、无上传的地区特点预案（v0.7） | ✅ 2026-08-10 |
| M8 | 洪涝预警维度 + 影响持续期（v0.8） | ✅ 2026-08-21 |

M8 实现记录（v0.8）：起因是 2026 年第 10 号台风美莎克（MAYSAK，2610）。它 7 月 3 日以强热带风暴级登陆海南陵水，7 月 4 日在越南广宁芒街二次登陆，全程未升格为台风级；登陆后结构不散反而在陆上重建眼区，眼区经广西东兴入境后在广西内陆滞留 26 小时，极端降雨引发六蓝、云表等水库溃坝。截至 8 月 21 日，南宁、贵港两地遇难 159 人、失联 10 人，其中水库溃坝洪灾遇难 107 人，官方定性"弱级强灾"。

复盘当时本项目的逻辑，暴露三处结构性缺陷：（1）`suggestLevel` 由 `windLevel` 单独决定档位，美莎克登陆时 10 级→黄色，减弱为热带低压后→蓝色（四档最低），而那正是伤亡最集中的时段；（2）致灾变量降雨根本不在数据模型里；（3）7 月 7 日停编后台风从 JSON 消失，页面回落"风平浪静"，而遇难数字在此后六周从 39 升至 159。另注：本仓库 7 月 10 日才建立，比美莎克停编晚三天，故其轨迹从未进入过存档——页面查不到它是工具没有历史功能，与数据可得性无关。

对应三处修改：`fetch-typhoon.mjs` 接入第四个源（中央气象台预警发布平台），抓暴雨/山洪灾害/山洪风险/地质灾害/台风五类在效预警，按 `alertid` 前 6 位行政区划代码聚合到省，输出 `alerts` 与 `recentlyEnded` 字段；预警与台风的关联走"该省已发台风预警（多台风并存按最近者归属，上限 1500km）或省中心距实况点/预报路径 ≤450km"两条规则——是台风↔省份的关联，不涉及用户位置，隐私边界不变。页面新增「02 雨情」区块与实况并列，档位改取 max(风力档, 洪涝档)，洪涝档更高时显式标注"台风等级只反映风速，不代表致灾程度"；停编台风保留最长 7 天影响持续期，其间影响省份仍有洪涝预警在效则维持风险姿态而非"风平浪静"。预警抓取设 60s 总预算并可降级为 `ok:false`，绝不拖垮台风实况这一主产物；两个数据工作流的校验步骤同步覆盖新字段。旧 schema 缓存与演示数据下雨情区块自动隐藏，向后兼容。

M7 实现记录（v0.7）：将用户反馈中的“不同地区应有不同预案”拆为六类可叠加风险场景，而不是采集省市或 GPS。用户可手动多选沿海与滨海城区、海岛与渔区、城市低洼区、山区与河谷、江河湖库周边、内陆乡村；每类建议随蓝→黄→橙→红递进，并与全国通用清单合并。地区选择和地区事项勾选只存在页面内存，刷新即清空；不读取 GPS、不通过 IP 推断位置、不填写精确地址、不上传。既有主题与全国通用清单进度仍仅保存在浏览器本机。

M5 实现记录：起因是 #0013 号热带低压期间浙江源命名滞后（媒体已称"红霞"而单源页面仍显示 NAMELESS）。`fetch-typhoon.mjs` 升级为三源并行抓取——中央气象台为主源出轨迹，浙江源备份轨迹、交叉校验并补全风圈，JMA（RSMC 东京，西太命名权威）交叉校验兼命名择优：任一源率先正式命名即采用，JMA 英文名经内置西太命名表对照中文名（查不到回退英文，待国内源跟进覆盖）。跨源按国际编号（未命名系统按中心距离 ≤350km）匹配，定位/强度比对按各机构时次差放宽容差，避免把发布时间差误判为分歧。产物 JSON 新增 `sources`、`verification`、`trackSource`、`nameNote` 字段（向后兼容，旧缓存仍可渲染）；页面 hero 新增三态校验行（一致/分歧/单源），徽标、通知栏、页脚同步明示多源状态。三源中任一轨迹源存活即可出数据，全挂才保留上一版。

M6 实现记录（v0.6）：路径图新增**远洋视图**。此前"远海段截断不显示"只按经纬度过滤路径点（>140°E 或 <10°N），未设兜底——2613 号台风"白海豚"生成于 169.6°E，整条路径被全部截掉，图上只剩底图、看不到台风。改为按**当前位置**决定视图：当前位置在近海窗口内仍走近海视图（远海历史照旧截断，角注提示"部分远海路径未显示"）；当前位置在窗口外则整条路径改用远洋视图——只框路径、按底图宽高比出图（并设 12 经度最小跨度，避免新生台风只有两三个点时放大到失真），以经纬网代替海岸线给出位置读数，并加一块**小地图**（游戏式）——把世界底图、整条路径与"当前图幅"框缩进一块面板，副标题给出距大陆直线距离，一眼看清台风在地球上的位置。

小地图**三档尺度**（全球 / 亚太 / 西北太平洋，默认亚太），面板右上角 －／＋ 切换，档位存 localStorage，到头一侧置灰；SVG 内补了 role/tabindex 与回车空格，键盘可用，键盘换档后焦点还回原按钮（鼠标点击不还，免留焦点框），另套透明热区解决窄屏点不准。初版只画中国海岸线，但"这块陆地是哪儿"对地理小白并不成立——现按日本 / 菲律宾 / 澳大利亚 / 夏威夷等地标加中文注记，标注贴边自动内收，标题行按实测文字宽自动缩字号避免压住按钮。底图 `assets/basemap.js` 由 `scripts/build-basemap.mjs` 离线生成（Natural Earth 1:110m Land，public domain，抽稀后 15 KB 静态坐标，运行时零依赖），只含陆地轮廓不含国界线。

小地图**自动避让**：候选网格逐格打分，压住路径点重罚、压住当前位置更重罚，同分再取离路径与九段线插图最远的一格；可放置区域先让开顶部经度标注行、左侧纬度标注列与底部 HTML 图例/角注。九段线插图反过来硬避让小地图（读其 `data-rect`），并隐藏被自己盖住的经纬网标注，避免露出半截度数。两块面板因此总是落在路径两侧的空白象限。

M4 实现记录：`scripts/fetch-typhoon.mjs`（零依赖 Node）抓取浙江台风路径系统 → `data/typhoon.json`，Actions 每 3 小时定时 + 手动触发；前端 fetch 实时 JSON、失败降级内置演示数据并显著标注；地图视窗随真实路径动态扩展（远海段截断不显示）；预警档位按"是否趋近沿海 + 当前风力"预选参考级（最高只预选到橙色，红色须由用户或官方判断）；多台风切换与无台风空态。上线于 GitHub Pages。

## 八、风险与边界

1. **数据准确性**：演示数据 ≠ 实况。已用页头横幅 + 页脚双重免责声明。
2. **过度依赖风险**：任何第三方页面都可能滞后，预案区首条永远是"服从当地政府统一安排"。
3. **地图精度**：示意图不用于导航或撤离路线判断。

## 九、验收清单（M3 自审依据）

2026-07-10 自审通过（本地预览 + DOM/计算样式断言逐项验证）：

- [x] 无控制台报错；主题切换、预警切换、清单勾选、路径悬停全部可用
- [x] 移动端 375px 宽度不横向滚动
- [x] 深浅两主题下四级预警色对比度可读（token 级验证两套色板均正确生效）
- [x] 刷新后勾选状态保留（localStorage，按"来源级:序号"跨级共享勾选）
- [x] `file://` 直接打开可用（无 fetch 依赖）

自审中发现并修复 3 个 bug：

1. 当前位置点填充色被 `.tp circle.dot` 更高优先级选择器覆盖，显示为普通白点 → 抬高当前点选择器优先级；
2. tooltip 锚定了带缩放动画的光晕圆，定位漂移 → 改为锚定实心点；
3. `map-wrap` 为 `overflow:hidden`，地图边缘路径点的 tooltip 被裁切 → 增加水平钳位与上缘翻转。
